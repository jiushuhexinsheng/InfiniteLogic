# 本文件已废弃 — 依赖已迁移至 pyproject.toml。使用 uv 管理：
# This file is deprecated — deps migrated to pyproject.toml. Use uv:
#
#   uv sync              # 生产依赖
#   uv sync --extra dev   # 生产 + 开发依赖
#
# 旧 pip 方式（不推荐）/ Legacy pip (not recommended):
#   python -m pip install -r requirements.txt

httpx>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
aiosqlite>=0.19.0
rich>=13.0.0
fastembed>=0.3.0
qdrant-client>=1.9.0
sentence-transformers>=3.0.0
pypdf>=4.0.0
docx2txt>=0.8
beautifulsoup4>=4.12.0
duckduckgo-search>=6.0.0
rank-bm25>=0.2.2
jieba>=0.42.1
loguru>=0.7.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
streamlit>=1.32.0
prometheus-client>=0.20.0
pytest>=8.0.0
pytest-asyncio>=0.23.0